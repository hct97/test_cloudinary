<%= form_with model: @song, url: songs_path do |f| %>

  <%= f.label :name %>
  <%= f.text_field :name, class: "form-control" %>

  <%= f.hidden_field :picture_cache %>
  <%= f.file_field :picture %>

  <%= f.submit "Submit", class: "btn btn-primary" %>
<% end %>
